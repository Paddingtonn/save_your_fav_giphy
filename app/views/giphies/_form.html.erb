<%= form_with(model: giphy, local: true) do |form| %>
  <% if giphy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(giphy.errors.count, "error") %> prohibited this giphy from being saved:</h2>

      <ul>
      <% giphy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
